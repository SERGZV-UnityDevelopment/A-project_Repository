 //--------------------------------------------------------------------------------------------- Выполненно ---------------------------------------------------------------------------------------------------    //----------------------------------------------------------------------------------------------- Задачи -----------------------------------------------------------------------------------------------------  // Объекты в инвентаре стакуются не как надо а по очереди подбора // Теперь если подобрать сначала нож а потом топор то топор займёт место впереди как и должен а купона с ножом сдвинет в очереди, однако если сначала поднять топор а потом нож то не понятно // куда девается нож. Скорее всего он просто не подбирается // Скрипт PickUp строчка 167  // Нестакуемые одинаковые предметы складываются в кучу исправить  // После изменений отображаются не те картинки на складе  // В инвентаре в категории всё отрисовка объектов должна сортироватся сначала по категороиям затем по нрмеру важности в собственной категории. // Когда же я включаю категория то сортирвка должна производится по соответствующей категории. Видимо для этого для каждой категории и есть разные массивы // 1) В общем инвентарь обстоит так. Есть места на складе которые просто заполняются по очереди для лучшей производительности. // 2) Есть "люди купоны" Которых мы сортируем в очереди таким образом чтобы они отображали красиво очерёдность в инвентаре // 3) В категории "всё" номер купона и номер ячейки это одно и то же однако для других категорий мы просто берём срез из очереди купонов отсеивая другие категории // получается что например для категории строительсвто мы берём купонов с 12 по 25. И уже этих купонов надо присваивать для каждой отдельной ячейки инвентаря // Метод FindASituablePlace в скрипте PickUp Как раз находит подходящее место для купона в категории "все" Если категория не красиво отображает предметы надо смотреть что в нём не так  // Игрок не отключается во время открытого инвентаря   // Переделать меню под unity UI Со старым больше не работает  // Строчки с которыми надо разобратся  // TargetNameText> 12 public Text TargetNametext;			                    // Сюда ложим текст кторый будет в GUIText // TargetNameText> 23 GuiText = TargetNametext.transform;	                    // ложим трансформацию GUIText,a в GuiText // PodgonEkrana> 18 posX = GetComponent<GUITexture>().pixelInset.x;             // Ложим в переменную posX // PodgonEkrana> 19 posY = GetComponent<GUITexture>().pixelInset.y;             // Выравниваем гуи текстуру по позиции переменной Posy равной нулю. // PodgonEkrana> 20 shirina = GetComponent<GUITexture>().pixelInset.width;      // Выравниваем ширину гуи текстуры по ширине экрана // PodgonEkrana> 21 vysota = GetComponent<GUITexture>().pixelInset.height;      // Выравниваем высоту гуи текстуры по высоте экрана // PodgonEkrana> 34 GetComponent<GUITexture>().pixelInset = newRect;      using UnityEngine; public class Notes : MonoBehaviour {  } 